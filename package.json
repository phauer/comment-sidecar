{
  "name": "comment-sidecar",
  "version": "1.0.0",
  "description": "Lightweight, tracking-free, self-hosted comment service for web spaces only supporting PHP and MySQL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-with-proxy": "browser-sync start --proxy 'localhost' --files 'src' --no-open",
    "watch": "echo 'Open http://localhost/playground.html in your browser!' && browser-sync start --files 'src' --no-ui"
  },
  "author": "Philipp Hauer",
  "devDependencies": {
    "browser-sync": "^2.27.10"
  }
}
